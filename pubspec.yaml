name: note_names
description: Note Names visualizer.
publish_to: "none"

version: 0.2.0

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ^3.38.0

dependencies:
  diacritic: ^0.1.6
  flutter:
    sdk: flutter
  music_notes: ^0.24.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true
